# Benchmarks

```bash
npm run benchmark
```

| File | Purpose |
|---|---|
| `benchmark.js` | Article centerpiece — No Cache vs Semantic Cache |
| `results.md` | Published numbers + `/stats` example |
| `without-cache.js` / `with-cache.js` | Focused simulations |

See [results.md](./results.md).
